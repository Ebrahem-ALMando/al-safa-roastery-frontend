# Laravel API (no trailing slash). Used by server-side pages such as /verify/[order_number].
LAB_API_BASE_URL=http://127.0.0.1:8000

# Optional duplicate for client-side usage if you add browser fetches later:
# NEXT_PUBLIC_LAB_API_BASE_URL=http://127.0.0.1:8000

# Shown on the public verification header (optional).
# NEXT_PUBLIC_BRAND_NAME_AR="محمصة الصفا"
# NEXT_PUBLIC_BRAND_NAME_EN="Al Safa Roastery"

# Production: see env.production.example
